extend type Query {
    getOrderItems(userId: Int): [OrderItem]
}

type OrderItem {
    id: Int
    amount: Int
    funkoPop: FunkoPop
}

type Order{
    id: Int!
    orderItems: [OrderItem]
    status: Status
}
