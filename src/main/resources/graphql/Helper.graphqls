input SearchPaging {
    page: Int! = 0
    perPage: Int! = 15
}

type Paging {
    page: Int!
    perPage: Int!
    pageCount: Int!
    totalCount: Int!
}

input Price {
    from: Int
    to: Int
}

enum OrderBy {
    IdAsc,
    IdDesc,
    NameAsc,
    NameDesc,
    DescriptionAsc,
    DescriptionDesc,
    DateAsc,
    DateDesc,
    TypeAsc,
    TypeDesc,
    PriceAsc,
    PriceDesc,
    CategoryAsk,
    CategoryDesc,
    SeriesAsk,
    SeriesDesc,
}
