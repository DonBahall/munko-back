type Mutation{
    save(entity: ReviewInput): Review
    updateReview(entity: ReviewInput, userid: Int): Review
    deleteReview(entity: Int, userid: Int): Boolean
}

input ReviewInput{
    id: Int!
    userId: Int!
    funkoId: Int!
    review: String!
    star: Int!
}