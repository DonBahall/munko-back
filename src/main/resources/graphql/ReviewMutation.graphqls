type Mutation{
    save(entity: ReviewInput): Review
    updateReview(entity: ReviewInput): Review
    deleteReview(entity: Int): Boolean
}

input ReviewInput{
    id: Int!
    userId: Int!
    funkoId: Int!
    review: String!
    star: Int!
}